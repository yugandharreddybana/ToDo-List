spring.application.name=productivity-suite-backend

# Database
spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.gppcofxrfuxnmgvlpend
spring.datasource.password=ItzMeYugi@0809
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=todo_list
spring.jpa.hibernate.ddl-auto=none

# Flyway migrations
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.schemas=todo_list
spring.flyway.baseline-on-migrate=true

# Security / JWT
app.jwt.secret=ThisIsASuperSecretKeyThatIsAtLeast32BytesLong!!
app.jwt.access-ttl-seconds=900
app.jwt.refresh-ttl-seconds=604800

# CORS (middleware is on 4001, frontend on 3002/3006)
app.cors.allowed-origins=http://localhost:4001,http://localhost:3002,http://localhost:3006

# Server
server.port=8080
server.servlet.context-path=/api
